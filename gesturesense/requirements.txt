# GestureSense runtime dependencies
mediapipe>=0.10.9      # both legacy Solutions (<0.10.15) and Tasks API supported
opencv-python>=4.8.0
numpy>=1.24            # mediapipe pins its own compatible numpy range
PyYAML>=6.0

# Optional: CPU/memory readouts in the debug overlay
psutil>=5.9

# Development / testing
pytest>=7.4
